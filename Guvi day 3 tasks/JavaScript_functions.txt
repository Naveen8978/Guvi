
#1 JavaScript program to generate a random number between 0 and 10

<!DOCTYPE html>
<head>
    <title>One App</title>
</head>

<body>
    <script type="text/javascript">
    var a = Math.floor(Math.random() *11)
    console.log(a)
    </script>

</body>
</html>

--------------------------------------------------------------------------------------------------

#2 JavaScript program to write a function that will get two number and return a sum of it

<!DOCTYPE html>
<head>
    <title>One App</title>
</head>

<body>
    <script type="text/javascript">
    var a = Number(prompt("Enter the first number:"))
    var b = Number(prompt("Enter the second number"))
     function addSum(a,b){
         var c = a+b
         return c
     }
     d = addSum(a,b)
     console.log("The sum of "+ a +" and " +b+ " is: " +d)
    </script>
</body>
</html>

--------------------------------------------------------------------------------------------------------------------

#3 program to create a calculator using arithmetic operations as functions

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Calculator</title>
</head>
<body>
    <script type="text/javascript">
    var firstNum = Number(prompt("Enter the first number:"))
    var secondNum = Number(prompt("Enter the second number:"))
    var sel = String(prompt("Enter 'a' for Addition \n Enter 'b' for Subtraction \n Enter 'c' for Division \n Enter 'd' for multiplicatio"))
    
    function addNum(firstNum, secondNum){
        var result = firstNum + secondNum
        return result
    }
    function subNum(firstNum, secondNum){
        var result = firstNum - secondNum
        return result
    }
    function mulNum(firstNum, secondNum){
        var result = firstNum * secondNum
        return result
    }
    function divisionNum(firstNum, secondNum){
        var result = firstNum / secondNum
        return result
    }
    switch (sel){
        case "a":
            var sum = addNum(firstNum,secondNum)
            console.log("The sum of "+firstNum+" and "+secondNum+" is: "+sum)
            break;
        case "b":
            var diff = subNum(firstNum, secondNum)
            console.log("The difference of "+firstNum+" and "+secondNum+" is: "+diff)
            break;
      case "c":
           var div = divisionNum(firstNum, secondNum)
           console.log("The reminder upon dividing "+firstNum+" and "+secondNum+" is: "+div)
           break;
      case "d":
          var product = mulNum(firstNum,secondNum)
          console.log("The product of "+firstNum+" and "+secondNum+" is: "+div)
        }
    </script>
</body>
</html>


--------------------------------------------------------------------------------------------------------------------
#4 Do the following:
 a. create a function to sum two numbers and return a value
 b. Create a function that generates a random number between 0 and 10
 c. pass two values to the sum function, first value should be passed by the user and the other value should be passed generated by a random function

<!DOCTYPE html>
<head>
    <title>function</title>
</head>

<body>
    <script type="text/javascript">
    var firstNum = Number(prompt("Enter the first number:"))
    function ranNum()
    {
        var b = Math.floor(Math.random() *11)
        return b;
    }
    var secondNum = ranNum()
    function sum(firstNum, secondNum)
    {
        var add = firstNum + secondNum
        return add;
    }
    result = sum(firstNum,secondNum)
    console.log("The sum of "+firstNum+" and "+secondNum+ " is: "+result)
    </script>
</body>
</html>

